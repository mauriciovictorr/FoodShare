const express = require('express');
const router = express.Router();
const Solicitacao = require('../models/Solicitacao'); // model

// Rota para ver o formulário de solicitar (GET) e enviar o pedido (POST)
// Rota para listar "minhas solicitações"
// Rotas para aceitar/recusar

module.exports = router;
