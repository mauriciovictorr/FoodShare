<!DOCTYPE html>

<html lang="pt-BR">

<head>

  <meta charset="UTF-8">

  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <meta name="description" content="FoodShare - Conectando quem tem com quem precisa">

  <title><%= typeof title !== 'undefined' ? title : 'FoodShare' %></title>

  <link rel="preconnect" href="https://fonts.googleapis.com">

  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

  <link href="https://fonts.googleapis.com/css2?family=Onest:wght@400;500;600;700&display=swap" rel="stylesheet">

  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@20..48,400,0..1,0&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="/css/style.css">

</head>

<body class="app-layout">

  <div class="app-shell">

    <%- include('app-sidebar', { activeNav: typeof activeNav !== 'undefined' ? activeNav : '' }) %>

    <div class="app-main">

      <div class="app-top">

        <%- include('app-header') %>

        <% if (typeof pageSubtitle !== 'undefined' && pageSubtitle && (typeof headerVariant === 'undefined' || headerVariant !== 'home')) { %>

          <p class="app-page-lead"><%= pageSubtitle %></p>

        <% } %>

      </div>

      <main class="app-content">

